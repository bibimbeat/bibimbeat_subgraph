{
  "name": "Bibimbeat",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ bibimbeat/test",
    "create-local": "graph create --node http://localhost:8020/ bibimbeat/test",
    "remove-local": "graph remove --node http://localhost:8020/ bibimbeat/test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bibimbeat/test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.1",
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
